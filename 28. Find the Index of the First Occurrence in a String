/**
 * @param {string} haystack
 * @param {string} needle
 * @return {number}
 */
var strStr = function(haystack, needle) {
    // for(let i =0; i<haystack.length||needle.length;i++){
       const ocuurr = haystack.indexOf(needle);
       return ocuurr;
    
    return -1;
};
